# Shared

Reusable TypeScript code shared by the API, experience layer, shell, and micro apps lives here.

## Portal Metadata

`src/portal-metadata.ts` defines the shared metadata contract used by micro apps and the app registry.
