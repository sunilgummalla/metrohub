# UI

React UI applications live here.

This app owns browser-facing screens, components, client state, and user interactions.

## Local development

```powershell
cd js
pnpm install
pnpm --filter @money-money/ui dev
```

The Vite app runs on port `3002` by default.
