[project]
name = "metrohub-py"
version = "0.1.0"
description = "Python workspace for MetroHub."
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = []

[tool.uv]
package = false

[tool.uv.workspace]
members = [
  "apps/*",
  "packages/*",
]
