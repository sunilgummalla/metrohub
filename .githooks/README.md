# Git Hooks

These hooks enforce the repository workflow locally after installation:

```powershell
./devops/scripts/install-git-hooks.ps1
```

The canonical enforcement still lives in GitHub branch protection for `develop`.
