# MetroHub secrets — SHARED base stack. ONE file on the VPS:
#   /opt/metrohub/shared/metrohub-base.secrets.yaml
# Copy this template, fill in values, chmod 600, owned by deploy, NEVER committed.
#   sync-secrets.sh --stack metrohub-base --file metrohub-base.secrets.yaml --catalog scripts/secrets.base.catalog
# Generate strong values with `openssl rand -hex 24`. Each app_<env>_* pair MUST
# match the credentials embedded in that env's mongo_uri (mh-<env>.secrets.yaml).

mongo_root_username:
mongo_root_password:
app_dev_username:
app_dev_password:
app_prod_username:
app_prod_password:
