# Flutter Workspace

Flutter and Dart projects live here and use Melos.

- Applications: `apps/*`
- Shared packages: `packages/*`

Common commands:

```powershell
cd flutter
dart pub get
dart run melos bootstrap
dart run melos run analyze
dart run melos run test
```
